# Order-4 Terminal-24 Fingerprint Report

Status: Phase C first structural correlation
Date: 2026-05-24

## Artifacts

Generated by:

```powershell
python "scripts/analyze_order4_terminal24.py" --write
```

Machine artifact:

```text
results/order4_terminal24_fingerprints.json
```

Input artifacts:

```text
data/order4_normal_essential_880.json
results/order4_endpoint_spectrum.json
```

## Scope

This report studies the `236` essential square-mask pairs with terminal sum
`24` from the internal 880-square dataset.

Endpoint `24` is equivalent here to:

```text
t_max = 10
```

so the selected one-incidence mask values are all at least `11`, with exactly
one minimum value `11`.

## Selected-Value Signatures

| Sorted selected values | Count |
|---|---:|
| `(11, 12, 13, 15)` | 4 |
| `(11, 12, 13, 16)` | 4 |
| `(11, 12, 14, 15)` | 4 |
| `(11, 12, 14, 16)` | 8 |
| `(11, 12, 15, 16)` | 176 |
| `(11, 13, 14, 15)` | 8 |
| `(11, 13, 14, 16)` | 4 |
| `(11, 13, 15, 16)` | 16 |
| `(11, 14, 15, 16)` | 12 |

The dominant value signature is `(11, 12, 15, 16)`, occurring in `176` of the
`236` terminal-24 pairs.

## APD-Style Classes

The APD-style fingerprint used here is:

```text
(APD_1, APD_2, APD_3, APD_4)
```

where `APD_k` is the alternating sum over the 24 permutation diagonals of the
`k`-th power of the diagonal sum.

Summary:

| Quantity | Count |
|---|---:|
| APD-style classes across all 880 essential squares | 93 |
| APD-style classes hit by terminal-24 pairs | 25 |
| terminal-24 pairs lying in singleton APD classes | 0 |

Interpretation: APD separates the 880-square world into coarse classes, and
terminal-24 cases occupy a restricted subset of those classes. It is not yet a
complete characterization.

## Permutation-Diagonal Fingerprints

For each terminal-24 pair `(Q,M)`, the script computes the target permutation
diagonal set:

```text
{ p in S_4 : sum_i (Q[i,p(i)] - 10 M[i,p(i)]) = 24 }
```

Size distribution:

| Terminal diagonal set size | Count |
|---:|---:|
| 2 | 52 |
| 3 | 12 |
| 4 | 168 |
| 5 | 4 |

Subgroup test:

| Is subgroup of `S_4` | Count |
|---|---:|
| false | 16 |
| true | 220 |

Subgroup order profiles:

| Order profile | Count |
|---|---:|
| `1:1,2:1` | 52 |
| `1:1,2:1,4:2` | 8 |
| `1:1,2:3` | 160 |

The Durer-style Klein four profile `1:1,2:3` is therefore common among
terminal-24 cases: `160/236`. In the current canonical orientation, the exact
set `{0123,1032,2301,3210}` occurs `144` times.

## Quaterne Transport

Global source baseline:

```text
all 880 essential normal order-4 squares have 86 four-cell subsets summing to 34
```

This is a baseline fact, not a terminal-24 distinguisher.

Terminal quaterne counts after the ray:

| Terminal quaternes at sum 24 | Count |
|---:|---:|
| 86 | 4 |
| 90 | 8 |
| 95 | 8 |
| 96 | 184 |
| 98 | 4 |
| 107 | 16 |
| 111 | 12 |

The Durer/Sagrada transport count `96` is the dominant terminal-24 type:
`184/236`.

## Current Reading

Phase C now has a stronger working signal:

- endpoint `24` is not unique in the 880-square extension;
- terminal-24 cases heavily favor high-value signatures, especially
  `(11,12,15,16)`;
- most terminal-24 cases have target permutation diagonals forming a subgroup
  of `S_4`;
- the Klein-four profile is the main subgroup profile;
- the Durer/Sagrada quaterne count `96` is the dominant terminal transport
  count, but not the only one.

The next step is to compare these distributions against all non-24 endpoint
pairs, so we can tell which signals are genuinely characteristic of endpoint
`24` rather than common artifacts of one-incidence rays.
