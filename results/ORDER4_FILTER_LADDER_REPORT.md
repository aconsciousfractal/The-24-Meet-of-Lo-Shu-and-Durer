# Order-4 Filter Ladder Report

Status: Phase C enriched-filter intersection
Date: 2026-05-24

## Artifact

Generated by:

```powershell
python "scripts/analyze_order4_filter_ladder.py" --write
```

Machine artifact:

```text
results/order4_filter_ladder.json
```

## Filter Ladder

| Stage | Pair count | Square count |
|---|---:|---:|
| all essential square-mask pairs | 7040 | 880 |
| endpoint `24` | 236 | 236 |
| endpoint `24` + subgroup terminal diagonals | 220 | 220 |
| endpoint `24` + Klein-four order profile `1:1,2:3` | 160 | 160 |
| endpoint `24` + Klein-four + terminal quaterne count `96` | 152 | 152 |
| same filters + selected values `(11,12,15,16)` | 152 | 152 |
| same filters + exact canonical `V4` terminal set | 144 | 144 |

The selected-value condition is automatic after the first four filters:

```text
endpoint 24 + Klein-four + terminal quaterne count 96
=> selected values (11,12,15,16)
```

within this finite dataset.

## Strict 152-Class

The strict class is:

```text
endpoint 24
+ terminal diagonal order profile 1:1,2:3
+ terminal quaterne count 96
```

Mask counts:

| Mask | Count |
|---|---:|
| `1203` | 4 |
| `1320` | 14 |
| `2013` | 44 |
| `2130` | 25 |
| `3021` | 38 |
| `3102` | 27 |

Terminal diagonal sets:

| Terminal diagonal set | Count |
|---|---:|
| `{0123,1032,2301,3210}` | 144 |
| `{0123,0213,3120,3210}` | 8 |

## Exact Canonical `V4` Subclass

Adding the exact terminal diagonal set

```text
{0123,1032,2301,3210}
```

gives `144` pairs.

Mask counts:

| Mask | Count |
|---|---:|
| `1203` | 4 |
| `1320` | 12 |
| `2013` | 44 |
| `2130` | 24 |
| `3021` | 36 |
| `3102` | 24 |

## Durer/Sagrada Location

The Durer complement square is present in the essential dataset at canonical
index:

```text
174
```

After canonicalization, the Sagrada mask appears as:

```text
1203
```

The canonical Durer/Sagrada record is:

```text
terminal sum: 24
t_max: 10
selected values: 12,11,15,16
sorted selected values: 11,12,15,16
terminal diagonal set: {0123,1032,2301,3210}
terminal diagonal order profile: 1:1,2:3
terminal quaterne count: 96
terminal decomposition: 25 + 50 + 21
```

So the Durer/Sagrada case lies in both:

- the strict 152-class;
- the exact canonical `V4` subclass of size 144.

## Reading

The filter ladder does not isolate Durer/Sagrada uniquely. It gives a natural
finite class around it.

Current best phrasing:

```text
The Durer/Sagrada case is a member of a 144-pair canonical V4 terminal
subclass inside the 236 endpoint-24 pairs.
```

This is now a concrete object for the next pass: classify the 144-pair subclass
up to square symmetries, mask symmetries, APD classes, and quaterne
decomposition types.
