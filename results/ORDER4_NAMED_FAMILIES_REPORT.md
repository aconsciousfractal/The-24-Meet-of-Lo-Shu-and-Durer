# Order-4 Named-Family Comparison Report

Status: Phase C comparison with computational order-4 families
Date: 2026-05-24

## Artifact

Generated by:

```powershell
python "scripts/analyze_order4_named_families.py" --write
```

Machine artifact:

```text
results/order4_named_families.json
```

## Family Labels

The labels in this report are computational:

| Label | Meaning |
|---|---|
| associated | opposite cells sum to `17` |
| complement-fixed | `canonical(17-Q)=Q` |
| panmagic | all wrapping/broken diagonals sum to `34` |
| local_2x2 | all non-wrapping local `2 x 2` blocks sum to `34` |
| wrap_2x2 | all wrapping `2 x 2` blocks sum to `34` |
| most_perfect_proxy | panmagic and wrap_2x2 |

## Counts In The 880 Essential Dataset

| Family | Square count |
|---|---:|
| associated | 48 |
| complement-fixed | 352 |
| panmagic | 48 |
| local_2x2 | 48 |
| wrap_2x2 | 48 |
| most_perfect_proxy | 48 |

Combined family partition:

| Family combination | Count |
|---|---:|
| associated + complement-fixed | 48 |
| complement-fixed only | 304 |
| panmagic + local_2x2 + wrap_2x2 + most_perfect_proxy | 48 |
| none of these labels | 480 |

So, in this internal dataset:

- every associated square is complement-fixed;
- the panmagic/local-2x2/wrap-2x2/most-perfect-proxy family has size `48`;
- associated and most-perfect-proxy are disjoint.

## Associated Family And The Durer Cell

Associated family:

```text
48 squares
384 square-mask pairs
16 endpoint-24 pairs
16 exact canonical V4 pairs
```

The 16 exact canonical V4 associated pairs have:

```text
source type: S3
APD classes: (0,0,0,55296):8 and (0,0,0,-290304):8
```

The Durer/Sagrada 8-cell is exactly half of this associated exact-V4 family:

```text
8/16 associated exact-V4 pairs
APD class: (0,0,0,55296)
source type: S3
```

Associated exact-V4 square indices:

```text
112, 123, 174, 205, 289, 305, 359, 394,
475, 557, 627, 740, 789, 802, 834, 849
```

Durer-cell overlap:

```text
112, 174, 289, 359, 789, 802, 834, 849
```

## Panmagic / Most-Perfect-Proxy Family

The panmagic, local-2x2, wrap-2x2, and most-perfect-proxy labels coincide on
the same 48 squares.

This family also has:

```text
16 endpoint-24 pairs
16 exact canonical V4 pairs
```

But it is disjoint from the Durer/Sagrada cell and has:

```text
source type: S4
APD classes: (0,0,0,55296):8 and (0,0,0,-290304):8
```

Panmagic exact-V4 square indices:

```text
101, 116, 176, 200, 278, 304, 371, 392,
482, 536, 564, 645, 703, 747, 784, 827
```

## Reading

The Durer/Sagrada cell is not in the most-perfect/panmagic family. It is in
the associated family.

More precisely:

```text
Durer/Sagrada cell = one 8-pair APD half of the associated exact-V4 family.
```

The closest named-family description found so far is therefore:

```text
associated + complement-fixed + endpoint-24 exact canonical V4
+ APD class (0,0,0,55296)
```

within the internal 880-square dataset.
